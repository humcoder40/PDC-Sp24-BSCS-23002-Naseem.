Humayun Naseem           BSCS-23002

so basically u have to open two terminals
### Terminal 1 (To start the FastAPI server): uvicorn main:app --reload
you keep this terminal open after entering the command

### Terminal 2 (To run the test script): python test.py
in second terminal we run this then it will show the output
